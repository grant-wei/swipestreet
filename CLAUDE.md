# SwipeStreet Project Instructions

## Commands

**ALWAYS provide full paths when giving commands to run.** Never use relative paths or assume the user is in a specific directory.

Example:
- Good: `cd C:\Github\swipestreet\mobile && npx expo start --clear`
- Bad: `npx expo start --clear`
